/**
Refine the function getCharCount so that it returns the count of characters present in the str parameter it receives.
 */
let a = "Arina";
let b = "Lily";
let c = "Nanao";

function getCharCount(a) {
  return (length = a.length);
}

console.log(getCharCount(a));
console.log(getCharCount(b));
console.log(getCharCount(c));
