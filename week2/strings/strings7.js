/**
Refine the concatInitials function to return a string that combines the firstNameInitial followed by the lastNameInitial.
 */
let firstNameInitial = "Arina";
let lastNameInitial = "Belugina";

function concatInitials(firstNameInitial) {
  return firstNameInitial.charAt(0);
}
function concatInitials1(lastNameInitial) {
  return lastNameInitial.charAt(0);
}
console.log(concatInitials(firstNameInitial), concatInitials1(lastNameInitial));
